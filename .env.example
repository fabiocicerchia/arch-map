# Copy to .env and fill in. Never commit the real .env.
# arch-map reads Terraform state and Kubernetes resources via the terraform/
# kubectl CLIs already configured on your machine — no env vars are required.
# KUBECONFIG=
